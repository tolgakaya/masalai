/**
 * @masalai/ui — design tokens (`tokens.css`) + component library. Imported by
 * apps/web only; may import @masalai/shared for types. No data fetching, no
 * business logic — presentational contract only (engineering-handbook §3.1).
 *
 * Scaffolded EMPTY in S0. The night-first token set and components land in
 * Stream C (issue #4), built from docs/ux-design-plan.md §4/§7/§14.
 */

/** Placeholder marker so the package has a typed public surface until Stream C. */
export const UI_PACKAGE_NAME = '@masalai/ui' as const;

# DECISIONS — append-only log (protocol §3.3)

> Format: `YYYY-MM-DD | decision | why | links`. Never edit/delete a line (protocol §8.4);
> promoted entries get ` | promoted → ADR-xxxx` appended. Small decisions live here; big ones become ADRs.

2026-07-14 | Pin toolchain to handbook §2.1 baseline MAJORS, newest minor within: TS 5.9.3 (not 7.x), pnpm 10.34.5 (not 11), Prisma 6.19.3 (not 7), pino 9.14.0 (not 10), Vitest 3.2.7 (not 4) | handbook §2.2 "never day-one majors"; newer majors revisited via ADR later | handbook §2.1/§2.2, issue #1
2026-07-14 | Non-baseline pins at their latest (aligned): Next 16.2.10, React 19.2.7, Fastify 5.10.0, fastify-type-provider-zod 7.0.0, zod 4.4.3, BullMQ 5.80.2, Better Auth 1.6.23, Biome 2.5.3, Tailwind 4.3.2 (deferred, ui empty) | match handbook baseline majors, verified via `npm show` | handbook §2.1
2026-07-14 | pnpm activated via Corepack (`corepack prepare pnpm@10.34.5 --activate`), pinned in package.json `packageManager` | handbook §2.1 (Corepack), reproducible across local/CI/Railway | —
2026-07-14 | `.serena/` gitignored at repo root (Serena IDE cache, not project source) | low-stakes housekeeping; keeps repo clean | protocol §5.2
2026-07-14 | packages/db uses Prisma classic `prisma-client-js` generator (default node_modules output), imported as `@prisma/client`, NOT the new EA `prisma-client` (output-into-src) generator | new generator emits .ts into src → fights strict tsc (noUnusedLocals) + NodeNext resolution; classic output stays out of typecheck/lint. Revisit via ADR when Prisma 7 forces the new generator | Prisma 6.6 changelog, handbook §6.1
2026-07-14 | pnpm `onlyBuiltDependencies` allowlist in root package.json: @prisma/client, @prisma/engines, prisma, esbuild, lefthook | pnpm 10 blocks postinstall build scripts by default; Prisma needs its engine binaries. Explicit committed allowlist = reproducible across local/CI (vs interactive `pnpm approve-builds`) | pnpm 10 build-script policy
2026-07-14 | Enum policy for packages/db: lifecycle fields = Prisma/Postgres enums (closed sets; ALTER TYPE ADD VALUE is expand-safe, removals via expand→contract); content/config fields (art_style, locale, language) = String validated at app layer against a registry (open sets — adding a value needs no migration) | plan §10.4 styles are versioned data not schema; keeps migrations off the hot path for content growth | plan §4.5, issue #1
2026-07-14 | Domain enum value sets (Tolga-specified): CharacterStatus{draft,moderating,generating_sheet,awaiting_approval,approved,rejected,failed} · CharacterType{child,sibling,pet,custom} · StoryStatus{queued,writing,illustrating,narrating,assembling,ready,failed} · JobStage{plan,illustrate,narrate,assemble,export} · JobStatus{queued,running,succeeded,failed,dead} · ModerationStatus{pending,passed,rejected,error} · SubscriptionPlan{family,premium} · SubscriptionStatus{active,past_due,canceled,paused} · AssetKind & CreditReason from plan §4.5 | mirror ux §6.3/§6.5 lifecycles + scale-doc §4.3 DLQ (dead) + MoR/Paddle webhook vocab; error(moderation)=scan failed≠content rejected | plan §4.5, ux §6.3/§6.5, scale-doc §4.3
2026-07-14 | Added story_characters join table (story_id, character_id, position default 0, PK(story_id,character_id), index(character_id), FKs onDelete Cascade) — plan §4.5 stories row had no character link but API §4.6 POST /stories takes character_ids[] | Phase-2 multi-character (plan §2.2) + KVKK "which stories feature this character" deletion query; MVP writes 1 row/story, position 0 = primary hero | plan §4.5 (updated same PR), §4.6, §2.2
2026-07-14 | audit_log.user_id nullable | system-originated audit events have no acting user | plan §4.5
2026-07-14 | Adopted docs/railway-deployment.md as normative for infra/railway/** + the S0b staging-deploy session (handbook §5.3 defers to it for Railway mechanics); added Doc-Touch row (deploy/infra → railway-deployment.md + §5.3) and made it S0b's step-1 context pointer. Reconciled EXISTING code to it: (a) REDIS_URL handling centralized into shared buildRedisConnection() with family:0 — Railway IPv6-only private net gotcha #3 — replacing per-app inline connections; (b) api honors Railway-injected PORT and binds 0.0.0.0 (was API_HOST/API_PORT), §3.2. Dockerfiles/railway.*.json/.dockerignore/api-migrate-preDeploy remain PENDING for S0b (no infra/railway files exist yet) | new binding infra doc; pre-empt the two deploy gotchas while the code is fresh; walking skeleton re-verified green with family:0 | docs/railway-deployment.md §2/§3/§4/§5.3/§7, protocol §7
2026-07-14 | S0b infra/railway Dockerfiles — 4 deltas from railway-deployment.md §3 canonical form, all needed to work with our pnpm-10 + Prisma stack (verified by local docker build + walking-skeleton round-trip across api+worker images): (1) `pnpm deploy --legacy` (pnpm 10 else demands inject-workspace-packages=true); (2) `COPY tsconfig.base.json` into builder (turbo prune omits the extends-referenced root config → tsc loses esModuleInterop); (3) `@prisma/client`+`prisma` as DIRECT deps of apps/api & apps/worker (transitive-via-db isn't hoisted to /out top-level → Prisma can't resolve it) + regenerate client into /out after deploy (pnpm deploy doesn't carry the generated client); (4) `prisma` CLI moved to @masalai/db prod deps + db ships prisma/ via package "files" (schema+migrations in image for the api preDeploy `prisma migrate deploy`) | doc is a .NET→pnpm translation; these are the pnpm/Prisma specifics it didn't resolve. Record as deltas per doc §8 ledger | railway-deployment.md §3/§4/§8, issue #6 | promote → railway-deployment.md §3 note in weekly review
